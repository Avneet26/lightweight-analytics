import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Lightweight Analytics | Simple, Privacy-Focused Analytics",
  description: "Track your website analytics with a simple, lightweight, and privacy-focused solution. No cookies, no personal data, just the metrics you need.",
  keywords: ["analytics", "website analytics", "privacy", "page views", "tracking"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
